/**
 *  Operadores de comparação
 * > maior que
 * >= maior que ou igual a
 * < menor que
 * <= emnor que ou igual a
 * == igualdade (valor) ****
 * === igualdade restrita (valor e tipo)
 * != diferent (valor) ****
 * !== diferente estrito (valor e tipo)
 */

const comp = 10 > 5; // vai atribuir o valor na var "comp" com o resultado da comparação com boolean
console.log(comp);
