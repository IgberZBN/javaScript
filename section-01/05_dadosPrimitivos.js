// Dados primitivos

const nome = 'Gabrield'; //String
const num = 10; //Number
const numFloat = 10.5; //Number
let nomeFill; //undefine = nao aponta para nenhum local na memoria         /* Nao proposital
const nulo = null; //null = nao aponta para local nenhum local na memoria     Proposital */
const booleanoT = true; //boolean
const booleanoF = false; //boolean

console.log(typeof nome, num, numFloat, nomeFill, nulo, booleanoF, booleanoT);
